/**
 * Drizzle tables: the single source of truth for the database layout.
 *
 * Migrations and row types are both generated from this file, never the reverse.
 * Tables arrive in workload 4.
 */
export {};
