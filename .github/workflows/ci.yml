name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# The DB suites share one Neon test branch and empty its tables between tests, so two runs at once would collide.
concurrency:
  group: test-database
  cancel-in-progress: false

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun install --frozen-lockfile
      # The DB and end-to-end suites skip unless this secret names a Neon test branch.
      - run: bun run test:coverage
        env:
          TEST_DATABASE_URL: ${{ secrets.TEST_DATABASE_URL }}
      - uses: codecov/codecov-action@v5
        with:
          files: ./coverage/lcov.info
          token: ${{ secrets.CODECOV_TOKEN }}
